# url-shortener
A multi-tenant URL shortener built with Laravel featuring role-based access (Super Admin, Admin, Member), company management, invitation-only user onboarding, and scoped URL visibility per role.
